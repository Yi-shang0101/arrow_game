# 真实开发记录与提交前补充项

## 记录边界

用户先要求拆解思路，再明确要求“开始做 Python 游戏项目”。以下是这一次开发请求中的三个真实执行阶段，不是三次虚构的用户提问，也不是学生本人已经完成的人工试玩。工具：ChatGPT / Codex。学生后续可继续提出真实修改需求，并保留对话截图或摘录。

| 阶段 | 实际任务 | AI 完成内容 | 实际验证与结果 | 人工修改 |
| --- | --- | --- | --- | --- |
| 1 规则与关卡 | 根据前述拆解实现 Python 游戏基础逻辑 | 创建 `can_exit`、状态模型、求解器及三关数据；使用逆向添加法生成可解布局 | 三关分别 10、19、30 支箭头，求解器均可清空；11 项逻辑测试通过 | 本次未发生用户人工修改，待用户阅读与试玩 |
| 2 界面与动画 | 实现中文图形界面及作业要求的完整流程 | Pygame 开始页、游戏页、结果页，飞出动画、碰撞反馈、3 次失误、重开；额外增加提示 | 实际渲染截图已检查，中文可读，主要页面未发现文字越界；捆绑开源字体 | 本次未发生用户人工修改 |
| 3 测试与修复 | 用模拟点击验证真实事件处理和三关流程 | 增加 2 项 UI 自动化测试 | 首次 UI 测试出现浮点索引 TypeError；格子中心函数返回浮点数，模拟事件因此使整除结果仍为浮点数。AI 在坐标转行列时加入 int，之后 13 项测试全通过 | 修复由 AI 完成，不记为学生人工修改 |

注意：实际操作系统鼠标事件通常给出整数坐标，此问题由模拟输入触发。修复让坐标换算同时兼容测试中的浮点坐标。未声称真实桌面点击曾经必然崩溃。

## 关键设计与答辩要点

1. 为什么用二维列表？每个单格箭头天然对应一个格子，数据简单、容易检查。
2. 为什么路径判断从下一格开始？当前格子是自己，不能把自己当成阻挡。
3. 阻挡箭头方向重要吗？不重要，只要位于前方同一行或列，就阻挡。
4. 为什么不会越界？先检查行列范围，再访问列表；边缘向外时循环不执行。
5. 为什么求解器无需回溯？删除只会减少阻挡，当前合法的移除不会破坏后续可解性。
6. 为什么动画不能 sleep？主循环要持续处理窗口事件；动画依靠帧间隔更新。
7. 为什么动画期间锁定棋盘？防止重复消除、重复扣次数和状态竞争。
8. 重开为什么要逐行复制？直接赋值会共享原始关卡；这里只含不可变字符串和 None，逐行复制足够。
9. 有哪些状态？MENU、PLAYING、LEVEL_CLEAR、GAME_OVER、ALL_CLEAR。
10. 为什么只有清空后才能进下一关？状态切换在最后一支箭头飞出动画完成时触发。

## PSP 表（自行真实填写）

未计量学生投入时间，不编造预计或实际耗时。后续开始新阶段前先填预计值。

| 任务 | 预计（小时） | 实际（小时） | 差异（实际−预计） |
| --- | --- | --- | --- |
| 需求分析与游戏设计 | 待填 | 待填 | 待填 |
| Python 与图形库学习 | 待填 | 待填 | 待填 |
| 游戏界面实现 | 待填 | 待填 | 待填 |
| 路径与碰撞逻辑实现 | 待填 | 待填 | 待填 |
| 关卡设计 | 待填 | 待填 | 待填 |
| AIGC 辅助开发 | 待填 | 待填 | 待填 |
| 测试与修改 | 待填 | 待填 | 待填 |
| README 与博客撰写 | 待填 | 待填 | 待填 |
| 合计 | 待填 | 待填 | 待填 |

各行避免重复计时。例如 AI 辅助开发填写专门提问、核对回答的时间，编码修改时间计入对应实现阶段。

## 提交前需要本人完成

- 在自己的电脑上安装依赖，实际试玩每一关。
- 按 TEST_REPORT 中 T01—T06 亲自操作，补充实际结果与系统环境。
- 阅读关键代码，至少能解释本页的 10 个问题。
- 按真实需要进行修改，继续留下有意义的 Git 提交。
- 将项目推送到自己的 GitHub 仓库，检查文件与图片是否正常显示。
- 在博客补充学号、课程及作业链接、GitHub 链接、真实 AI 对话记录、PSP 与个人心得。
- 本文是开发记录，不是已经完成的课程博客，不应直接将“待填”内容当最终提交。

## 后续真实需求：按时间评价

用户要求：≤15s 为 A，≤25s 为 B，≤35s 为 C，>35s 闯关失败。

AI 修改：每关计时，游戏界面增加用时和倒计时，结果页显示等级与耗时，区分超时/失误失败；重开与下一关归零。计时使用未截断的帧间隔，防止低帧率使限时变慢；最后一次动画在慢帧中按实际完成时刻结算。飞出缩短到 0.25 秒，避免第三关仅动画就超过 A 的上限。依赖改为 pygame-ce 2.5.8，对应用户之前报告的 Python 3.14 安装问题。

验证：增加阈值和状态测试，以及超时结果页/评级渲染事件测试。当前共 22 项测试。用户人工修改及实际试玩仍待本人补充。

## 后续真实需求：B 以上解锁与首页选关

用户要求：前一关 B 及以上才能继续下一关，在首页加入选关以便反复挑战。AI 实现：第一关开放，前置关卡最佳时间≤25秒解锁下一关，首页展示最佳成绩和锁定原因；本次 C 结果页提供重试而不允许直接下一关。最佳成绩只在更快通关时更新，解锁状态永久保留，增加本地 JSON 自动保存及读写错误提示。新增 8 项测试，当前 30 项自动化测试通过；本人实机试玩结果待用户补充。

## 用户澄清：独立首页与选择关卡入口

用户指出希望首页有“选择关卡”按钮，点击后跳转到已制作的关卡选择页。AI 将两页分离，恢复首页标题、规则与箭头图案，保留“开始游戏”，新增“选择关卡”；选关页提供返回首页。结果页也可直接进入选关页面。新增导航与停止计时测试，31 项通过。

## 按用户标注调整首页布局

根据用户截图：标题、说明和按钮整体居中；开始游戏与选择关卡上下排列；鼠标悬停以绿色填充、白字、外轮廓和小箭头高亮；删除右侧独立卡片，将低对比度箭头直接绘制在背景。检查实际渲染和按钮居中位置，原有 31 项测试通过。

## 按新截图简化首页

用户要求将首页规则放到关卡开始时，开始游戏按钮比选择关卡更大。AI 将规则移至 READY 页面，确认后才进入 PLAYING 并开始计时；首页主按钮 360×78、字号27，次按钮280×58、字号21。增加说明页停留不计时、确认/重试/返回测试，32 项通过。

## 首页退出游戏按钮

用户要求在选择关卡下添加同尺寸、同高亮逻辑的退出选项。复用 home_button 绘制与命中检测，点击设置 running=False，由主循环正常调用 pygame.quit()。已用模拟点击检查退出，并检查悬停画面。

## 背景元素增强

按用户要求加深首页背景箭头至柔和绿色 #92B49D，线宽由4增至5；背景圆点改为 #B2C7AF，半径由3增至4。保持原位置和布局，实际渲染检查清晰度。

## 用户提供音频接入

用户提供“游戏音效(1).7z”，包含主菜单音乐、进行游戏音乐、倒计时、箭头飞出、箭头被阻挡、游戏成功和游戏失败。AI 解压并核对文件后，将 7 个文件放入 `assets/audio/`，新增 `AudioManager`：首页/选关/规则页播放主菜单音乐，确认开始挑战后切换进行游戏音乐并播放倒计时，点击箭头播放对应效果，状态进入通关或失败页播放结果音效并停止倒计时。音频设备不可用时自动降级静音。新增 3 项音频资源与状态路由测试，当前 35 项测试通过。

## 结果音效停止与按钮音效

用户要求完成关卡后播放的游戏成功音效在点击“下一关”“重试本关”“选择关卡”时立即暂停，并增加按钮点击音效。AI 将成功/失败音效保存为 result_channel，结果页按钮执行切换前调用 stop_result；所有界面按钮点击统一播放用户提供的 `点击按钮.wav`。新增 4 项音频测试，当前 39 项测试通过。

结果音效固定在专用混音通道，按钮音效不会复用该通道，确保点击结果页按钮后成功或失败音效真正立即停止。

## 规则说明页增加返回首页

用户要求点击“开始游戏”后不仅能返回选关，还能返回首页。说明页现在同时提供“返回选关”和“返回首页”，两个按钮都播放点击音效并保持原有高亮逻辑。

## 结果页返回首页与随机失败音效

用户要求成功/失败结果页均可返回首页，并提供三条新的失败音效。AI 在结果页增加“返回首页”按钮，沿用结果音效立即停止和按钮点击音效逻辑；失败状态从原有音效与三条新增音效共四条中随机播放。首页“退出游戏”从三条新增音效中随机播放一条，等待音效结束（最多 1.5 秒）后关闭窗口。新增音频资源完整性、四选一随机、退出音效和结果页首页导航测试；全套 40 项测试通过。

## 设置页：音量与显示模式

用户要求在“选择关卡”和“退出游戏”之间增加“设置”。AI 新增独立 SETTINGS 状态和入口，设置页提供音效音量、音乐音量两个 0%—100% 滑动条，并加入白天模式、护眼模式、夜间模式三种主题；拖动和切换立即生效，Esc 或“返回首页”可离开设置页。AudioManager 增加独立音乐/音效音量控制，主题颜色统一映射到首页、选关、规则、游戏和结果页面。新增设置页滑块、主题切换和导航测试，全套 41 项测试通过。

## 增加困难与专家关卡

用户要求增加较难关卡，并按难度配置挑战评分。AI 新增第 4 关“纵横迷阵”（7×7、困难）和第 5 关“极限交织”（8×8、专家），两关均通过求解器验证。普通关卡沿用 A≤15/B≤25/C≤35 秒；困难关卡使用 A≤25/B≤40/C≤55 秒；专家关卡使用 A≤35/B≤55/C≤75 秒。解锁仍要求前一关达到 B 或 A，但 B 的时间阈值随前一关难度变化。选关页改为两列卡片布局，显示每关难度和评级要求；旧版三关存档会自动补齐新增关卡空成绩。新增难度评级、难度解锁、旧存档迁移和五关流程验证，全套 44 项测试通过。

## 用户指定字体

用户提供 Google Fonts 的 Ma Shan Zheng 与 Noto Serif SC 引入链接，要求用于游戏界面。AI 下载对应的 `MaShanZheng-Regular.ttf` 和 `NotoSerifSC-Regular.ttf` 并放入 `assets/`，Pygame 启动时标题优先使用 Ma Shan Zheng，正文和按钮使用 Noto Serif SC；包含 `·` 分隔符的关卡信息行改用 Noto Serif SC 渲染，避免 Ma Shan Zheng 缺少该字形而显示方框。资源缺失时回退到随包 Noto Sans CJK，再回退到系统中文字体。字体资源与原有 Noto Sans CJK 一并遵循 `assets/OFL.txt` 中的 SIL Open Font License 1.1。修改后需重新检查中文渲染、按钮宽度和五关界面截图。

## 用户指定的字体组合与自动求解

用户进一步更新字体链接，要求同时使用 Ma Shan Zheng 与 Noto Serif SC。AI 将字体职责分开：Ma Shan Zheng 用于主标题和结果标题，Noto Serif SC 用于正文、按钮和数据标签。游戏页新增“自动求解 A”按钮和键盘快捷键 A；点击后调用当前棋盘的 `solve()` 顺序，沿用正常飞出动画、计时、音效与通关结算，自动过程中锁定棋盘手动点击，返回首页或重新开始仍可中断。新增逻辑和 UI 测试，当前共 47 项测试通过。

## 首页背景箭头交互

用户要求背景箭头也能互动。AI 为首页八个背景箭头增加圆形命中区域：鼠标悬停时提高颜色对比度和线宽，点击后播放按钮/飞出音效，并沿箭头方向发射一支装饰性动画箭头；动画不参与关卡计时，也不会改变关卡状态。新增 UI 测试验证命中、悬停状态和动画结束，全套测试更新为 48 项。
