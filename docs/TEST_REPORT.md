# 测试报告

环境：Linux、Python 3.12.14、pygame-ce 2.5.8；界面用 SDL dummy 驱动进行无窗口渲染和模拟鼠标事件测试。

结果：35 项 unittest 测试通过。原始输出见 `test_output.txt`。

| 编号 | 内容 | 预期 | 本次实际结果 |
| --- | --- | --- | --- |
| T01 | 点击无阻挡箭头 | 飞出后消除，不扣次数 | 通过；动画完成前仍保留数据，完成后删除 |
| T02 | 点击被阻挡箭头 | 箭头不消失，失误减 1 | 通过；动画期间额外点击被忽略，无重复扣除 |
| T03 | 边缘朝外箭头 | 正常消除，不越界 | 四个方向边界判断均通过；UI 通关过程中覆盖飞出 |
| T04 | 清空本关 | 通关并进入下一关 | 模拟鼠标完整清空三关，最后显示 ALL_CLEAR；继续返回第一关 |
| T05 | 耗尽失误 | 失败并允许重开 | 连续三次碰撞后 GAME_OVER；重开恢复原布局与 3 次机会 |
| T06 | 进行中重开 | 恢复布局与次数 | 部分消除后和动画进行时重开均验证；不会残留动画删除新棋盘 |

额外覆盖：远处阻挡、身后不阻挡、非法棋盘、空格及越界点击、死锁识别、原始关卡不变、提示超时、首页导航。

渲染检查：01_menu、02_game、04_failure、06_level3 已查看原始截图，未发现主要文字溢出和遮挡。其他结果截图由相同渲染程序生成。

限制：自动事件测试不代表本人手动试玩，未在用户的 Windows 桌面实机验证显示缩放、输入手感和动画流畅度；这些应在提交前补充。

## 手工复核栏

| 项目 | 实机结果（本人填写） |
| --- | --- |
| 操作系统、Python 版本、显示缩放 | 待填 |
| 第一关实际通关 | 待填 |
| 第二关实际通关 | 待填 |
| 第三关实际通关 | 待填 |
| 碰撞动画与失误扣除 | 待填 |
| 失败重开与中途重开 | 待填 |
| 空格与棋盘外点击 | 待填 |

## 限时评级更新验证

新增 8 项逻辑测试、1 项界面测试：15/25/35 秒精确边界及刚超过边界；35 秒仍可继续、超过即失败；最后动画恰好 35 秒完成算 C；慢帧内按真实完成时刻评级；飞出期间超时不再删除；结果页停止计时；重开/下一关归零；提示不暂停；超时结果页可以重开；第三关理论动画耗时 7.5 秒可获 A。

本次在独立 pygame-ce 环境运行全套测试，重新生成截图。用户电脑的 Python 3.14 尚需本人运行确认。

## 关卡解锁与首页选关更新

新增 7 项进度测试与 1 项 UI 流程测试：初始锁定、C 无法进下一关、恰好 25 秒的 B 解锁、顺序解锁第三关、重复挑战保留更优成绩和解锁状态、失败不覆盖成绩、文件保存/重新加载、损坏存档回退、写入失败提示、禁止写入未解锁关卡成绩、首页锁定按钮与选关事件。全部 30 项通过。选关首页的初始和已解锁画面均已检查。

## 独立首页修正

根据用户澄清，将首页与选关页分为 MENU / SELECT 两个状态；首页“开始游戏”进入第一关，“选择关卡”进入选关页，选关页按钮或 Esc 可返回首页。新增导航与选关页不计时验证；现共 31 项测试通过。已检查新版首页截图。

## 首页视觉更新

检查居中布局、上下按钮间隔及实际悬停截图 `10_home_hover.png`，无文本重叠。重新执行原有 31 项测试，全部通过。

## 开始前规则页

新增 READY 页面测试，等待60秒仍为0秒；点击棋盘不响应；Enter确认后计时；重试归零并回规则页；可返回选关。首页主按钮宽高大于次按钮。32项测试通过，首页与说明页截图已检查。

## 音效验证

检查 7 个音频资源均存在，SDL dummy 音频设备下可以加载 WAV/MP3；验证 MENU/READY 使用主菜单音乐，PLAYING 切换游戏音乐并启动倒计时，超时进入 GAME_OVER 后停止倒计时并恢复主菜单音乐；各音效调用在音频关闭后也不会抛出异常。35 项测试全部通过。
